import { RENDER_NEW_GRAPH_ON_SEARCH } from "../actions/UserActions/SettingsActions";

const SETTINGS = [
    {
        name: RENDER_NEW_GRAPH_ON_SEARCH,
        description: "Add to current graph on new search",
        value: true
    }
];

export default SETTINGS;